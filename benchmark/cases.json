[
  {
    "id": "completed-work",
    "title": "Completed work with one skipped check",
    "prompt": "Turn this internal note into a user-facing agent reply. Preserve every important fact.\n\nThe cache-key defect was remediated in src/cache.ts and src/api.ts. All 42 unit tests completed successfully, and the linting process also completed successfully. The browser-based verification could not be executed because Chrome is not installed. No action is required from the user.",
    "required": [
      { "label": "both files", "pattern": "src/cache\\.ts[\\s\\S]*src/api\\.ts|src/api\\.ts[\\s\\S]*src/cache\\.ts" },
      { "label": "42 tests passed", "pattern": "42[\\s\\S]{0,40}(passed|successful|successfully)" },
      { "label": "lint passed", "pattern": "lint(?:ing)?[\\s\\S]{0,50}(passed|successful|successfully|completed)" },
      { "label": "browser check skipped", "pattern": "browser[\\s\\S]{0,50}(not run|did not run|could not|skipped|unable)" },
      { "label": "Chrome cause", "pattern": "Chrome[\\s\\S]{0,40}(not installed|unavailable|missing)" },
      { "label": "no user action", "pattern": "no (user )?action|nothing (else )?(is )?required" }
    ],
    "expect_next": false
  },
  {
    "id": "blocked-deploy",
    "title": "Blocked deployment with one required action",
    "prompt": "Turn this internal note into a user-facing agent reply. Preserve every important fact and the exact command.\n\nThe deployment procedure was not initiated because the AWS session expired. The local production build completed successfully. No production files or services were modified. The user must execute `aws sso login --profile billing-prod` and tell the agent when authentication finishes.",
    "required": [
      { "label": "deployment did not happen", "pattern": "deploy[\\s\\S]{0,40}(did not|not (?:start|started|initiated)|failed|blocked)" },
      { "label": "expired AWS session", "pattern": "AWS session[\\s\\S]{0,30}expired|expired[\\s\\S]{0,30}AWS session" },
      { "label": "local build passed", "pattern": "local production build[\\s\\S]{0,30}(passed|successful|successfully)" },
      { "label": "production unchanged", "pattern": "no production (files or services|files|services)[\\s\\S]{0,40}(changed|modified)|production files or services (remain|were) unchanged" },
      { "label": "exact login command", "pattern": "aws sso login --profile billing-prod" },
      { "label": "report completion", "pattern": "(tell me|tell the agent|let me know)[\\s\\S]{0,40}(finishes|finished|complete|done)" }
    ],
    "expect_next": true
  },
  {
    "id": "queue-choice",
    "title": "Recommendation with measurable limits",
    "prompt": "Turn this decision note into a user-facing recommendation. Preserve every important fact.\n\nFor this small service, utilize a PostgreSQL-backed queue at the present time. PostgreSQL is already in operation, so this choice eliminates the necessity for an additional service and is sufficient for the current load. Redis can facilitate lower latency and additional queue features, but it introduces more operational work. Reconsider Redis if measured queue delay exceeds 500 ms or throughput exceeds 1,000 jobs per second.",
    "required": [
      { "label": "PostgreSQL recommendation", "pattern": "(use|choose|recommend)[\\s\\S]{0,40}PostgreSQL-backed (job )?queue" },
      { "label": "no new service", "pattern": "avoid[\\s\\S]{0,30}(new|another|additional) service|eliminates[\\s\\S]{0,30}(new|another|additional) service" },
      { "label": "current load", "pattern": "current load" },
      { "label": "Redis benefits", "pattern": "Redis[\\s\\S]{0,80}lower latency[\\s\\S]{0,60}(more|additional) queue features" },
      { "label": "operations cost", "pattern": "Redis[\\s\\S]{0,140}(operations|operational) (work|overhead|cost)" },
      { "label": "500 ms limit", "pattern": "500 ms" },
      { "label": "1,000 jobs limit", "pattern": "1,000 jobs per second" }
    ],
    "expect_next": false
  }
]
